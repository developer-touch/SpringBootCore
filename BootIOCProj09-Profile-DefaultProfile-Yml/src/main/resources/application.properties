spring.application.name=BootIOCProj09-Profile

# default Profile =>if specified any active,if it is not available in any application.properties/yml file the it 
#                   take main application.yml/properties file as fallback properties/yml 

#@Profile("default") -> if we don't specify any active profile and also not configure any child profile (spring.profile.include)
#                       or committed then @Profile("default") take place in the application 

#=> we can't set multiple spring bean as default it take ambiguity problem 

#spring.profiles.active=staging

spring.profiles.include=oracle

#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.datasource.url=jdbc:mysql://localhost:3306/ankit
#spring.datasource.username=root
#spring.datasource.password=ankit@123